[phases.setup]
  nixPkgs = ["nginx"]

[phases.build]
cmds = ["mkdir -p dist && cp -r assets privacy-policy index.html about.html style.css doom.mp4 doom.png Zafer1.png CNAME app-ads.txt dist/"]

[static]
  directory = "dist"
